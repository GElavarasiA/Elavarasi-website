pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                echo 'Building image...'
            }
        }

        stage('Run Docker Container') {
            steps {
                echo 'Running container...'
            }
        }
    }
}
